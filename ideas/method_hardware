在该项目中，在硬件的设计上秉持着 尽可能减少传感器 以及 高集成度 的设计理念，以此来最大程度地降低成本和复杂度，在软件上则尽可能保证模块之间以及机器人与主机之间的解耦，以此提高灵活性。

该机器人需要具备以下能力：图像拍摄，无线通讯，旋转和移动，粗略位置估计。

因此，该机器人使用esp32 cam作为主控芯片，其有着优秀的性能，体积小巧同时有着Wi-Fi可用于无线通讯。
其还有带fifo相机接口可以用于连接相机模块进行拍照，增加集成度和减少阻塞。
虽然引脚较少，但是在复用特殊引脚，比如uart，init，的前提下足够该项目使用。

相机则使用ov3660，因为其支持像素分辨率包含1024*768，该像素分辨率接近Depth Pro的输入像素1536*1536，可以减少过度拉伸产生的浪费，或压缩产生的信息丢失，
且其FOV约为45度，且镜头产生的畸变几乎没有，可以在不产生畸变的前提下尽可能有着较大的拍摄扫描范围。
另外其支持高速输出，且与esp32cam的接口匹配。

旋转和移动是使用l298N加上n15减速电机完成，搭配全向轮来完成，在第一版中我们使用了步进电机来（cite 详见appendix），但是由于其速度慢，可能打滑，电耗高，因此最后使用直流电机记时加imu的方式来完成。

imu使用mpu6050，它自带DMP芯片不需要浪费性能进行额外的积分。而且被广泛使用，有着丰富的资料

图像拍摄由ov3660+led完成

无线通讯使用esp32 cam Wi-Fi通讯，基于TCP协议。esp32cam会连上与主机相同网络，esp32cam生成一个tcp 服务端，主机可以连接该服务端来获取小车信息并控制小车。

旋转和移动是使用l298N加上n15减速电机，搭配全向轮来完成，通过电机正反转来前进后退和左右原地旋转

粗略orientation估计是基于imu的位置估计加上小车的运动延时，电机经过测试，在5.5V的电压下，会以10cm/s的速度前进，
小车有orientation用于计算实时相对orientation
在坐标系A下

相对旋转则是直接由imu给出
而位置则是
P_now^A = V*dt*R^A + P_last^A
,
由于小车的orientation估计并不准确，且误差会积累，因此小车不会输出绝对坐标，而是只会给出重置坐标系后的相对坐标，坐标系则是由merge后根据merge误差给出精确位置后重置。


小车也是根据其功能由4个Task，wifi反馈，机器人控制，照片读取，位置估算，由于读取拍照fifo和imu，以及计算位置都需要用到大量性能，因此很可能造成堵塞，因此它是使用RTOS开发，以此保证通讯优先，减少堵塞

WIFI反馈的优先级最高，以此保证通讯的流畅，防止阻塞，它平时会挂起，当有通讯包收到 则会开始运行。

机器人控制则是由状态机加抽象对象属性控制，平时挂起，当对象属性改变后，则会开始运行，基于对象属性更改机器人运动状态

照片是间隔20ms轮询，这是由于ov3660输出清空后约5ms会填满一次fifo（产生新的照片），作为冗余，20ms读取一次。
位置估算是10ms，因为mpu6050的输出速率较快，另外，速率越高dt越小，精度越高
位置和照片都是是被读取是因为它们读取并计算一次花费时间较长，若先收到指令再读取可能会产生阻塞，所以让它们实时读取，输出时wifi模块会读取它们预先读取并存储在变量中的值。另外为了保证资源共享的稳定性，位置信息和照片都会被互斥量保护。

综上，机器人小车有如下几个功能可供主机端调用
拍照
前进/后退 x cm
左右旋转至 x degree
    前进和后退是通过记时来做的
    左右旋转是根据imu的，当当前角度接近目标角度时（<20 degree），旋转速度会变慢，当当前角度和目标角度误差小于正负5时，会停下
设定当前orientation为新参考坐标系
获取当前相对orientation



蜂群式扫描与中心化处理架构
该项目采用蜂群式扫描，也就是多台小车并行扫描，用户可以根据环境的大小和时间，成本，等条件，自行决定需要部署的小车的数量，更加灵活。
由于使用了ai模型，因此对算力有所要求，但是若每一个单独的slam小车都使用高算力npu来部署ai模型，则成本会大幅上升，如图X所示，因此本文将使用数据中心化处理的方法，所有slam小车均可视为执行器和传感器，其本身只用于移动和采集数据，而所有的数据都会被远程传输到终端高算力计算机上统一处理和规划，以此来有效降低成本

运行流程则如图X所示，
主机端从小车端收到照片和粗略估计的位置后，会先使用depth pro转化为深度图，再把深度图转化为散点云，
接着会基于粗略估计的位置把单张照片的散点云进行后处理并合并到全局散点云上，并基于合并误差的反馈给出精确位置
然后会把散点云转化为2d地图，用于判定哪里能通过，哪里有block，哪里有未扫描的区域，cost又是多少
最后pathplan会根据精确位置和2d地图给出小车的路径以及运行指令

当全部的机器人都完成扫描，也就是没有可规划路径时，扫描结束，并把全局散点云输出为结果

===============

小车系统采用 RTOS 架构，包含 4 个任务：
WiFi反馈Task、机器人控制Task、照片读取Task、位置估算Task。

系统维护一个统一的全局状态对象，保存机器人全部运行状态，包括：
运动状态、运动 PWM、现实移动速度、当前最新照片、当前相机参数、当前 IMU 旋转输出、错误日志、参考坐标系、当前相对位姿。

WiFi反馈Task 优先级最高，只负责通讯收发和状态对象的读取/修改，不直接执行耗时的 IMU 读取、照片采集和位置计算。它平时挂起，在收到通讯包时被唤醒。

机器人控制Task 优先级第二高，平时挂起。当 WiFi反馈Task 修改运动相关参数后被唤醒，根据对象中的运动状态和 PWM 更新机器人运动方式。

照片读取Task 与 位置估算Task 优先级相同且较低。照片读取任务按固定周期轮询相机，持续更新最新照片缓存；位置估算任务按固定周期读取 IMU 并更新相对位姿。二者都在后台执行，以避免在收到指令后再临时读取硬件造成阻塞。

所有共享状态必须使用互斥锁保护，且任何共享结果都必须在完整计算完成后一次性写入，禁止分阶段写入，以保证 WiFi反馈Task 对外读取到的始终是完整一致的数据。



===============================================



首先map的每个区块格有三种状态：block，costing，unknown，
block就是不能通过，costing就是可以通过，根据其一些属性会增加cost，costing越高就代表路的状态越差，越难通过，costing为0~255，大于等于255的区块也会被标记为block。unknown就是还没有扫描过的区域
小车只能在costing的格子里运动

block设定为5cm*5cm，这是因为小车尺寸约为10*10*11cm，其自转圆的直径为2*sqrt（5^2+5^2），约为14cm，为了冗余我们假象小车为一个15*15*15cm的立方体，5*5cm的小格会很方便，只要8格为costing，就代表小车可通过。

2D Map的处理是这样的
先对所有的散点云进行一次地面层判定，然后再每个区块判定如果地面层上方的阈值 Hth 以内有障碍，则将该区块设定为block，tau的值被设定为15cm，因为小车车高11cm，留一点冗余。

没有障碍就可以算costing，算costing 可以通过特征值和特征向量来算，
首先会通过特征向量来计算坡度，
由于小车的爬坡能力约为30degree，超过30度后，小车的速度就会下降，影响粗略位置估计，
因此我们会把0~30度线性映射为0~255的costing，超出则标记为block。
另外也会根据地面的特征值来计算复杂程度，当归一化的特征值越大就是约复杂，当三特征值相同时（最大复杂度时，也就是5*5*5cm的空间均匀分布），归一化为sqrt（1/3）约为0.6，当复杂度为最大值的1/10时，为sqrt（1/30）约为0.18，所以复杂度阈值被设定为0.18，1/10是因为小车的通过性是0.5cm，即可以轻松跨越0.5cm的障碍且不影响位置估计，因此复杂度为最大值的1/10

复杂度的映射是非线性的，因为复杂度和现实障碍的映射是平方关系，所以使用公式
cost(c) = 255 * (c / 0.18)^2
c是复杂度



障碍进行分类讨论
目前有如下几种可能出现的环境类型
台阶/墙壁
悬空障碍
平面/斜坡
坑洞/悬崖
小障碍（复杂度）

每种的处理方法
台阶/墙壁和悬空障碍是被一起处理的，首先会对整个地图进行地面连续性判定，由于台阶，墙壁的上半部分都会被转换为悬空障碍，由于台阶和墙壁都是连续的，所以必然会存在15cm以内的空中障碍判定，判定为block

空中障碍则是看有没有在阈值Hth以内，如果不在其中，则忽略，如果在其中，则block

平面/斜坡和小障碍（复杂度）就是通过复杂度和特征向量的映射来算costing

坑洞/悬崖，由于扫描视角原因，往往悬崖和坑洞都没法直接被扫描，中间往往会有视角差，所以往往会无法扫描到悬崖或坑洞的崖壁，这样就会被标记为unknown，这可以保证小车不会踩上去，而靠近或完整扫描后，悬崖和坑洞也会被转化为斜坡或不连续地面，进一步转化为costing或block

区块的合并

所有的区块都有它与真实世界对应的坐标
为了节约性能，每一帧扫描，map merge只会返回有变动的散点云，这时候2dmap会根据新的数据进行覆盖合并，这是因为理想状态下场景散点云会因为多次merge消除误差，会越来越精准，所以将会使用新的数据覆盖旧的。当然unknown区块也会被覆盖为costing或block，也起到作为map的全局合并的作用

