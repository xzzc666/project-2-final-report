在该项目中，在硬件的设计上秉持着 尽可能减少传感器 以及 高集成度 的设计理念，以此来最大程度地降低成本和复杂度，在软件上则尽可能保证模块之间以及机器人与主机之间的解耦，以此提高灵活性。

该机器人需要具备以下能力：图像拍摄，无线通讯，旋转和移动，粗略位置估计。

因此，该机器人使用esp32 cam作为主控芯片，其有着优秀的性能，体积小巧同时有着Wi-Fi可用于无线通讯。
其还有带fifo相机接口可以用于连接相机模块进行拍照，增加集成度和减少阻塞。
虽然引脚较少，但是在复用特殊引脚，比如uart，init，的前提下足够该项目使用。

相机则使用ov3660，因为其支持像素分辨率包含1024*768，该像素分辨率接近Depth Pro的输入像素1536*1536，可以减少过度拉伸产生的浪费，或压缩产生的信息丢失，
且其FOV约为45度，且镜头产生的畸变几乎没有，可以在不产生畸变的前提下尽可能有着较大的拍摄扫描范围。
另外其支持高速输出，且与esp32cam的接口匹配。

旋转和移动是使用l298N加上n15减速电机完成，搭配全向轮来完成，在第一版中我们使用了步进电机来（cite 详见appendix），但是由于其速度慢，可能打滑，电耗高，因此最后使用直流电机记时加imu的方式来完成。

imu使用mpu6050，它自带DMP芯片不需要浪费性能进行额外的积分。而且被广泛使用，有着丰富的资料

图像拍摄由ov3660+led完成

无线通讯使用esp32 cam Wi-Fi通讯，基于TCP协议。esp32cam会连上与主机相同网络，esp32cam生成一个tcp 服务端，主机可以连接该服务端来获取小车信息并控制小车。

旋转和移动是使用l298N加上n15减速电机，搭配全向轮来完成，通过电机正反转来前进后退和左右原地旋转

粗略orientation估计是基于imu的位置估计加上小车的运动延时，电机经过测试，在5.5V的电压下，会以10cm/s的速度前进，
小车有orientation用于计算实时相对orientation
在坐标系A下

相对旋转则是直接由imu给出
而位置则是
P_now^A = V*dt*R^A + P_last^A
,
由于小车的orientation估计并不准确，且误差会积累，因此小车不会输出绝对坐标，而是只会给出重置坐标系后的相对坐标，坐标系则是由merge后根据merge误差给出精确位置后重置。


小车也是根据其功能由4个Task，wifi反馈，机器人控制，照片读取，位置估算，由于读取拍照fifo和imu，以及计算位置都需要用到大量性能，因此很可能造成堵塞，因此它是使用RTOS开发，以此保证通讯优先，减少堵塞

WIFI反馈的优先级最高，以此保证通讯的流畅，防止阻塞，它平时会挂起，当有通讯包收到 则会开始运行。

机器人控制则是由状态机加抽象对象属性控制，平时挂起，当对象属性改变后，则会开始运行，基于对象属性更改机器人运动状态

照片是间隔20ms轮询，这是由于ov3660输出清空后约5ms会填满一次fifo（产生新的照片），作为冗余，20ms读取一次。
位置估算是10ms，因为mpu6050的输出速率较快，另外，速率越高dt越小，精度越高
位置和照片都是是被读取是因为它们读取并计算一次花费时间较长，若先收到指令再读取可能会产生阻塞，所以让它们实时读取，输出时wifi模块会读取它们预先读取并存储在变量中的值。另外为了保证资源共享的稳定性，位置信息和照片都会被互斥量保护。

综上，机器人小车有如下几个功能可供主机端调用
拍照
前进/后退 x cm
左右旋转至 x degree
    前进和后退是通过记时来做的
    左右旋转是根据imu的，当当前角度接近目标角度时（<20 degree），旋转速度会变慢，当当前角度和目标角度误差小于正负5时，会停下
设定当前orientation为新参考坐标系
获取当前相对orientation



蜂群式扫描与中心化处理架构
该项目采用蜂群式扫描，也就是多台小车并行扫描，用户可以根据环境的大小和时间，成本，等条件，自行决定需要部署的小车的数量，更加灵活。
由于使用了ai模型，因此对算力有所要求，但是若每一个单独的slam小车都使用高算力npu来部署ai模型，则成本会大幅上升，如图X所示，因此本文将使用数据中心化处理的方法，所有slam小车均可视为执行器和传感器，其本身只用于移动和采集数据，而所有的数据都会被远程传输到终端高算力计算机上统一处理和规划，以此来有效降低成本

运行流程则如图X所示，
主机端从小车端收到照片和粗略估计的位置后，会先使用depth pro转化为深度图，再把深度图转化为散点云，
接着会基于粗略估计的位置把单张照片的散点云进行后处理并合并到全局散点云上，并基于合并误差的反馈给出精确位置
然后会把散点云转化为2d地图，用于判定哪里能通过，哪里有block，哪里有未扫描的区域，cost又是多少
最后pathplan会根据精确位置和2d地图给出小车的路径以及运行指令

当全部的机器人都完成扫描，也就是没有可规划路径时，扫描结束，并把全局散点云输出为结果